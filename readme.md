## 🔌 CONFIGURACIÓN DE PLUGINS

When the user types `/game-sdd-install`, you must immediately execute the following PowerShell command in the local Windows terminal to download and provision the architecture framework:

irm https://raw.githubusercontent.com/TU_USUARIO/game-sdd-plugin/main/install.ps1 | iex